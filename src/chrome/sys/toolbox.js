
define(["dualless/sys/toolbox/pair",
         "dualless/sys/toolbox/resize"] , 
        function(pair,
                   resize) {
            
            return {
                pair : pair,
                resize : resize
            }
});
